version: '3.7'

name: purr-ui

networks:
  backend:
    name: aggr_backend
  frontend:
    name: purr_frontend

services:
  front:
    container_name: purr-ui-dev
    image: purr-ui-dev
    build:
      context: ../.
      dockerfile: ./docker/Dockerfile.dev
    volumes:
        - ..:/usr/src/app
    ports:
      - 8080:8080
    env_file:
      - ../.env
    networks:
      - backend
      - frontend
    restart: always
    deploy:
        resources:
            limits:
              cpus: '0.5'
              memory: 1028M
            reservations:
              cpus: '0.25'
              memory: 720M

  cors:
    container_name: purr-cors
    image: testcab/cors-anywhere
    environment:
      - NODE_ENV=development
      - PORT=8070
    ports:
      - 8070:8070
    networks:
      - frontend
    restart: always
    deploy:
        resources:
            limits:
              cpus: '0.1'
              memory: 128M
            reservations:
              cpus: '0.05'
              memory: 72M